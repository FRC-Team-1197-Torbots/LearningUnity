using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Solenoid
{
    public bool set(bool on)
    {
        return on;
    }

    public double setPulseDuration(double durationSeconds)
    {
        return durationSeconds;
    }
}